import { AppShell } from '@/components/layout/AppShell';

export default function HomePage() {
  return (
    <AppShell
      title="AI-WOC Clean MVP Core"
      description="Locked starter workflow foundation: Capture → Confirm → Issue → Generate → Review / Copy / Send."
    />
  );
}
